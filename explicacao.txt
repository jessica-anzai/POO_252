Funcionario f1 = new Funcionario();

Funcionario = classe, tipo, estrutura de dados
f1 = objeto, varivel de instância

objeto é instancia da classe.

INSTÂNCIA{
    new = alocação de memoria
    Funcionario() = método construtor
    valor da variavel = estado
}


foreach(tipo variavel in vetor)

Um objeto só existe quando existe a instanciação

List<Funcionario> vetF = new List<funcionario>();
     <gererics>            tipo do construtor

.append (python inserir valor) == .Add(obj)

ENCAPSULAMENTO
class Conta{
    private double saldo;
}


//get = buscar ;;; set = alterar
//Console.WriteLine(c1.GetSaldo());

//PROPRIEDADE
public double Saldo{
    get{
        return saldo;
    }
    set{
        saldo = value;
    }
}

construtor é um metodo sem retorno, inicializa os atributos do objeto
prop = monta declaração de atributos de forma implicita com encapsulamento get e set
ctor = monta construtor

var = tipo implicito

HERENÇA
Poligono : Forma -> : significa 'extends'

    <- GENERALIZAÇÃO <-
Forma <- Retângulo <-  Quadrado
    -> ESPECIALIZAÇÃO ->

PRIVATE vira PROTECTED, para que as classes filho consiga acessar o atributo da classe pai. Atributo do filho permanece PRIVATE (ultima classe)